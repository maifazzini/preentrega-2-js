/* while (isNaN(espesorVidrio) || espesorVidrio < 1 || espesorVidrio > 50) {
    if (nombre>50){
        alert("El numero es mayor a 50. Recorda que ninguna linea admite acristalamiento con tanto grosor");
    }else{
        alert("No se ingreso un número. Intente nuevamente.");
    }
    espesorVidrio = parseInt(prompt("Ingrese el espesor del vidrio colocado en milimetros"));
} */

    /* function ChequeoEspesor( ingreso) {
    let nombre 
    for(nombre = parseFloat(prompt(ingreso)); isNaN(nombre) || nombre> 50; nombre = parseFloat(prompt(ingreso))){
        if (nombre>50){
            alert("El numero es mayor a 50. Recorda que ninguna linea admite acristalamiento con tanto grosor")
        }else{
            alert("No se ingreso un número. Intente nuevamente.");
        }
        
    }
    return nombre;
} */
/* function Chequeo( ingreso) {
    let nombre = parseFloat(prompt(ingreso));
    while (isNaN(nombre)) {
        alert("No se ingreso un número. Intente nuevamente.");
        nombre = parseFloat(prompt(ingreso));
    }
    return nombre;
} */